name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - run: python -m pip install -e '.[dev]'
      - run: fly-sniff-source-fetch --help
      - run: fly-sniff-dna02-readme-fetch --help
      - run: ruff check src tests
      - run: pytest -q
